const DashNav=(props)=>{

    return(
        <div>
            <nav>
                <ul>
                    <li>Hello {props.username}</li>
                </ul>
            </nav>
        </div>
    )
}

export default DashNav;